    <div class="navbar navbar-default navbar-fixed-top">
      <div class="container">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <a class="navbar-brand" href="/"><%= app_name %></a>
        </div>
        <div class="collapse navbar-collapse">
          <ul class="nav navbar-nav">
            <li <%= active(root_path) %> ><a href="/">Dashboard</a></li>
            <li <%= active(invoices_path) %> ><%= link_to t('menu.invoices'), invoices_path %></li>
            <li <%= active(clients_path) %> ><%= link_to t('menu.clients'), clients_path %></li>
            <li <%= active(memos_path) %> ><%= link_to 'Memo', memos_path %></li>
          </ul>
          <ul class="nav navbar-nav navbar-right">
            <li class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown">Anno: <%= current_year %> <b class="caret"></b></a>
              <ul class="dropdown-menu">
                <li><%= link_to "2013", change_year_path(2013), :method => :post %></li>
                <li><%= link_to "2014", change_year_path(2014), :method => :post %></li>
              </ul>
            </li>

            <!--
            <li class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown"><%= t('menu.settings') %> <b class="caret"></b></a>
              <ul class="dropdown-menu">

              </ul>
            </li>
              -->
            <!-- 
            <li>
              <%= link_to t('.logout') + " | " + I18n.locale, signout_path, :title => current_user.email, :method=>:delete %>
            </li>
              -->
            <li class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown"><%= current_user.name %> <b class="caret"></b></a>
              <ul class="dropdown-menu">
                <li>
                  <%= link_to t('menu.logout'), signout_path, :title => (current_user.email + " - " + I18n.locale.to_s), :method=>:delete %>
                </li>
                <li class="divider"></li>

                <li><%= link_to t('menu.footers'), footers_path %></li>
                <li><%= link_to t('menu.company_data'), company_path(current_user.company.id) %></li>
                <% if current_user.admin? %>
                  <li class="divider"></li>
                  <li><%= link_to t('menu.companies'), companies_path %></li>
                  <li><%= link_to t('menu.users'), users_path %></li>
                <% end %>

              </ul>
            </li>


          </ul>

        </div><!--/.nav-collapse -->
      </div>
    </div>
